# PE-RAG — runtime + test dependencies
# Python 3.11+. Install:  pip install -r requirements.txt

# --- LLM / RAG stack (LangChain) ---
langchain>=0.3,<0.4
langchain-core>=0.3,<0.4
langchain-community>=0.3,<0.4
langchain-text-splitters>=0.3,<0.4
langchain-openai>=0.2,<0.4   # generation via OpenAI-compatible OpenRouter (ChatOpenAI + base_url)
langchain-chroma>=0.1.4,<0.3
chromadb>=0.5,<0.7
openai>=1.40,<2              # OpenAI-compatible client used by langchain-openai (-> OpenRouter)
rank-bm25>=0.2.2,<0.3        # sparse retrieval for hybrid search

# --- Local models (embeddings + reranker), no API ---
# Embeddings: BAAI/bge-large-en-v1.5 · Reranker: BAAI/bge-reranker-base|large.
# Lazy-imported; unit tests use fakes and don't need it. On a very new Python a torch
# wheel may be unavailable — use Python 3.11-3.12 (as the Docker image does) to install.
sentence-transformers>=3.0,<6

# --- Config / validation ---
pydantic>=2.7,<3
pydantic-settings>=2.3,<3
python-dotenv>=1.0,<2

# --- Reranking (P10) — OPTIONAL local cross-encoder; lazy-imported with a graceful
#     fallback to RRF order if absent (may lack a wheel on the newest Python). Install with:
#         pip install "sentence-transformers>=3,<6"
# sentence-transformers>=3.0,<6

# --- Serving / UI (Phase 2) ---
fastapi>=0.110,<1
uvicorn[standard]>=0.29,<1
streamlit>=1.36,<2

# --- Dev / test ---
pytest>=8,<10
