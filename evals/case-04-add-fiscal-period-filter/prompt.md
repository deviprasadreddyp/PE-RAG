# Prompt — case 04 (build eval)

> Paste into a fresh chat with `agents.md` loaded.

Add support for fiscal-period **range** filtering to the retriever, so a question like "How did
Microsoft's margins change from 2023 to 2025?" retrieves only MSFT filings whose fiscal period falls
in that range. Build it as a new feature.
