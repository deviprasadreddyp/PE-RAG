# Copy to .env and fill in. .env is git-ignored — never commit real keys.
ANTHROPIC_API_KEY=sk-ant-...
# Optional: finance-domain embeddings. If unset, the pipeline falls back to a local model.
VOYAGE_API_KEY=
